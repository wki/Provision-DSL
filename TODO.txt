TODO for provisioning
---------------------

Generell:
  * Rolle User, Group neu
  
  * Package: installer attribute OS-X: port, Ubuntu: apt, aptitute
             Provision::DSL::Installer::Port | Apt | Aptitude | ...
             Methoden: latest_version, installed_version, install(version)
  
  * Mechanismus für Generelle Voreinstellungen, z.B.:
    Defaults(
        File    => { user => 'me', group => 'we' },
        Package => { installer => 'apt' },
        ...
    );
    
    beim Instantiieren werden die Default-Werte pro Klasse _zuerst_ gesetzt.
    
  * File: Methode, um Config-Dateien vom Original abweichend zu editieren
    File '/etc/path/to/conf' => {
        change => [
            {
                line_like   => qr{...},     # Muster muß passen und eindeutig sein
                replacement     => ...          # Ersetzung. String oder sub{}
                comment => '#',     # Kommentarzeichen falls von "#" abweichend, [ '/*', '*/' ] falls klammernd
            },
        ],
    };

Execute:
  * cwd attribute

Perlbrew:
  * beim Installieren: laufen lassen von local/bin/perlbrew self-install

new Entities:
  * cron (path, arguments, environment)

Idea:
  * How to handle dir lists?
      - { path, permission, user, group }
      - Object ???
